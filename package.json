{
  "name": "cypresswork",
  "version": "1.0.0",
  "description": "",
  "main": "01_normal mode_spec.cy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:local_T": "cypress run --headed --no-exit --browser chrome --config-file cypress.local.config.js",
    "cy:local": "cypress run --headed --no-exit --browser chrome --config-file cypress.config.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "cy2": "^4.0.9",
    "cypress": "^15.8.1",
    "cypress-mochawesome-reporter": "^4.0.2"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "xlsx": "^0.18.5"
  }
}
